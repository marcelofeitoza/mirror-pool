{
  "name": "mirror-pool-circuits",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Groth16 membership circuit for mirror-pool ZK-deniable initiation (clean-room, circomlib + snarkjs only).",
  "scripts": {
    "build": "bash build.sh",
    "build:transaction": "bash build_transaction.sh",
    "build:association": "bash build_association.sh",
    "fixture": "node gen_fixture.js",
    "fixture:transaction": "node gen_transaction_fixture.js",
    "fixture:association": "node gen_association_fixture.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "ethers": "^5.8.0",
    "snarkjs": "^0.7.5"
  }
}
